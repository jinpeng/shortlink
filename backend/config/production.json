{
  "host": "shortlink.com",
  "port": 80,
  "auth_token": "This is a simple auth token, but long enough to be safe enough!",
  "database": {
    "url": "postgres:///testdb?sslmode=disable",
    "max_connections": 16
  }
}
